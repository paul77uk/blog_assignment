<%= form_with model: @post, url: user_post_path(@post), method: "patch" do |f| %>
<%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :body %>
    <%= f.text_field :body %>
    <%= f.submit :send %>
  <% end %>